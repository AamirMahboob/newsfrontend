export const colorObject = {
  main: "#0a101a",
  dark: "#111827",
  light: "#BABABA",
  contrastText: "#FFFFFF",
  textColor: "#111111",
};
// 111827
const customPrimaryColors = {
  main: colorObject.main,
  dark: colorObject.dark,
  light: colorObject.light,
  contrastText: colorObject.contrastText,
  textColor: colorObject.textColor,
};

const customSecondaryColors = {
  main: colorObject.main,
  dark: colorObject.dark,
  light: colorObject.light,
  contrastText: colorObject.contrastText,
  white: "#FFFFFF",
};

export { customPrimaryColors, customSecondaryColors };
